doctype 5
html
	head
	title
		piano
	link(rel='stylesheet', type='text/css', href='styles/main.css')
	script(src='scripts/jquery-1.8.2.min.js')
	script(src='scripts/jquery.mobile.min.js')
	meta(name='viewport', content='width=device-width,user-scalable=no, initial-scale=1, maximum-scale=1, user-scalable=0,orientation=landscape')
	script
		$(function(){

			$('.button')
			.bind('mousedown touchstart', function(){
				$(this).addClass('active');
			})
			.bind('mouseup touchend', function(){
				$(this).removeClass('active');
			});
		});

body.piano
	//
		.top-line		
			.first-octave
				.button
				.button
			.second-octave
				.button
				.button
				.button
	.bottom-line
		.button#A
		.button#B
		.button#C
		.button#D
		.button#E
		.button#F
		.button#G

	
